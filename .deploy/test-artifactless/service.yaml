apiVersion: v1
kind: Service
metadata:
  name: nginx-artifactless-service
  namespace: test
spec:
  type: LoadBalancer
  selector:
    app: nginx-artifactless
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
